apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-kavita
  labels:
    app: kavita
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: kavita
